name: Test non-root locations

on:
  push:
    branches: [main]
jobs:
  test-workflows:
    name: See if workflows run from non root locations
    runs-on: ubuntu-latest
    steps:
      - name: Install Poetry
        run: |
          echo "hello world"
      
